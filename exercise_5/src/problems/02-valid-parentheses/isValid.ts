/**
 * Valid Parentheses
 *
 * Mając string `s` złożony wyłącznie ze znaków '(', ')', '{', '}', '[' i ']',
 * zwróć `true`, jeśli nawiasy są poprawnie zamknięte i zagnieżdżone.
 *
 * Przykłady:
 *   isValid("()")       // -> true
 *   isValid("()[]{}")   // -> true
 *   isValid("(]")       // -> false
 *   isValid("([)]")     // -> false  (zła kolejność zamknięcia)
 *   isValid("{[]}")     // -> true
 *   isValid("")         // -> true  (pusty string jest poprawny)
 *
 * Oczekiwana złożoność: O(n) czasu, O(n) pamięci (podpowiedź: stos).
 */
export function isValid(s: string): boolean {
  throw new Error("Not implemented");
}
