/**
 * Two Sum
 *
 * Mając tablicę liczb całkowitych `nums` oraz liczbę `target`, zwróć indeksy
 * dwóch elementów, których suma daje `target`.
 *
 * Założenia:
 * - Dla każdego wejścia istnieje dokładnie jedno rozwiązanie.
 * - Nie możesz użyć tego samego elementu tablicy dwa razy.
 * - Kolejność zwróconych indeksów nie ma znaczenia w tym zadaniu (testy sortują wynik).
 *
 * Przykład:
 *   twoSum([2, 7, 11, 15], 9) // -> [0, 1]  (2 + 7 = 9)
 *
 * Oczekiwana złożoność: O(n) czasu, O(n) pamięci (podpowiedź: hash mapa).
 */
export function twoSum(nums: number[], target: number): number[] {
  throw new Error("Not implemented");
}
