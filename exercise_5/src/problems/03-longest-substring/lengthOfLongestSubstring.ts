/**
 * Longest Substring Without Repeating Characters
 *
 * Mając string `s`, zwróć długość najdłuższego podciągu (kolejnych znaków),
 * który nie zawiera powtarzających się znaków.
 *
 * Przykłady:
 *   lengthOfLongestSubstring("abcabcbb") // -> 3  ("abc")
 *   lengthOfLongestSubstring("bbbbb")    // -> 1  ("b")
 *   lengthOfLongestSubstring("pwwkew")   // -> 3  ("wke")
 *   lengthOfLongestSubstring("")         // -> 0
 *
 * Oczekiwana złożoność: O(n) czasu (podpowiedź: sliding window + mapa/Set
 * ostatnich pozycji znaków), unikaj rozwiązania O(n^2) ze sprawdzaniem
 * każdego podciągu osobno.
 */
export function lengthOfLongestSubstring(s: string): number {
  throw new Error("Not implemented");
}
